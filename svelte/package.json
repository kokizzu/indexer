{
  "name": "indexer-svelte",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run dev",
    "watch": "npm run build -- --watch",
    "dev": "node build.js --watch --serve",
    "build": "node build.js",
    "build:prod": "node build.js --minify",
    "serve": "node build.js --serve-only"
  },
  "devDependencies": {
    "chokidar": "^3.6.0",
    "esbuild": "^0.27.4",
    "esbuild-svelte": "^0.9.4",
    "svelte": "^5.55.7"
  }
}
